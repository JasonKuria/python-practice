# Create a list 'a' with some integer elements.
a = [10, 20, 30, 40]
# Use the built-in sum() function to calculate
#the sum of all elements present in the list,
res = sum(a)
# Print the calculation outcome
print(res)